<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration
    PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
    "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">

<generatorConfiguration>
    <properties resource="generator-mysql.properties" />
    <context id="MySqlContext" targetRuntime="MyBatis3" defaultModelType="flat">
        <property name="beginningDelimiter" value="`" />
        <property name="endingDelimiter" value="`" />
        <property name="javaFileEncoding" value="UTF-8" />
        <!-- 为模型生成序列化方法 -->
        <plugin type="org.mybatis.generator.plugins.SerializablePlugin" />
        <!-- 为生成的 Java 模型创建一个 toString 方法 -->
        <plugin type="org.mybatis.generator.plugins.ToStringPlugin" />
        <!-- 生成 mapper.xml 时覆盖原文件-->
        <plugin type="org.mybatis.generator.plugins.UnmergeableXmlMappersPlugin" />
        <commentGenerator type="com.cafe.generator.mybatis.CommentGenerator">
            <!-- 是否去除自动生成的注释: true 是, false 否 -->
            <property name="suppressAllComments" value="true" />
            <property name="suppressDate" value="true" />
            <!-- 自定义注释生成器 CommentGenerator 相关参数: true 生成自定义注释 -->
            <property name="addRemarkComments" value="true" />
        </commentGenerator>

        <!-- 数据库连接配置: 驱动、地址、用户名、密码 -->
        <jdbcConnection driverClass="${driver}"
                        connectionURL="${url}${table-prefix}${module}${parameter}"
                        userId="${username}"
                        password="${password}">
            <property name="nullCatalogMeansCurrent" value="true" />
        </jdbcConnection>

        <!--
            false(默认): 把 JDBC 的 DECIMAL、NUMERIC 类型解析为 java.lang.Integer
            true: 把 JDBC 的 DECIMAL、NUMERIC 类型解析为 java.math.BigDecimal
		-->
        <javaTypeResolver>
            <property name="forceBigDecimals" value="true" />
        </javaTypeResolver>

        <!-- Model 实体模型生成路径 -->
        <javaModelGenerator targetPackage="${project-package}.${module}.${model-package}"
                            targetProject="temp" />

        <!-- Mapper 映射文件生成路径 -->
        <sqlMapGenerator targetPackage="${project-package}.${module}.${xml-package}"
                         targetProject="temp" />

        <!-- Mapper 接口生成路径 -->
        <javaClientGenerator targetPackage="${project-package}.${module}.${mapper-package}"
                             targetProject="temp"
                             type="XMLMAPPER" />

        <!-- tableName="%" 表示: 生成全部表-->
        <table tableName="%">
            <generatedKey column="id" sqlStatement="MySql" identity="true" />
            <!-- 去除生成类名的 Mall 前缀, 注意 ^ 后面第一个字母大写 -->
            <domainObjectRenamingRule searchString="^Mall" replaceString="" />
        </table>
    </context>
</generatorConfiguration>