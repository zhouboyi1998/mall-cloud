server:
  port: 18093

spring:
  application:
    name: mall-elasticsearch
  profiles:
    active: dev

logging:
  level:
    org.springframework.data.elasticsearch: DEBUG
    org.elasticsearch.client: TRACE