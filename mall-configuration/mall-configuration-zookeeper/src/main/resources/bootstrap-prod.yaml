spring:
  # 微服务配置
  cloud:
    # Zookeeper 配置
    zookeeper:
      # 连接地址
      connect-string: 192.168.159.128:2181
      # Zookeeper 配置中心
      config:
        # 项目配置根节点
        root: config
        # 公共配置根节点
        defaultContext: mall
        # 配置根节点和运行环境之间的分隔符
        profileSeparator: ","
