# Apollo 配置
app:
  id: mall-cloud
apollo:
  meta: http://192.168.44.128:8080
  bootstrap:
    enabled: true
    namespaces: mall,mall-test,${spring.application.name},${spring.application.name}-test
  cacheDir: ./cache/apollo
