# Apollo 配置
app:
  id: mall-cloud
apollo:
  meta: http://127.0.0.1:8080
  bootstrap:
    enabled: true
    namespaces: mall,mall-dev,${spring.application.name},${spring.application.name}-dev
  cacheDir: ./cache/apollo
