spring:
  application:
    name: eureka-cluster-2

server:
  port: 8762

eureka:
  instance:
    hostname: cluster-2
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://cluster-1:8761/eureka, http://cluster-3:8763/eureka
