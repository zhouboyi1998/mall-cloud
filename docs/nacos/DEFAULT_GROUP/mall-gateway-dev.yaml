spring:
  # Spring Security 安全配置
  security:
    oauth2:
      resourceserver:
        jwt:
          # 获取 JWK 公钥接口
          jwk-set-uri: http://127.0.0.1:${server.port}/mall-security/jwk/jwk-set
  # Redis 配置
  redis:
    host: 127.0.0.1
    port: 6379
    password: 123456
    database: 0
